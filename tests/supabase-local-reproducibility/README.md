# Supabase Local Reproducibility Fixtures

The negative mutation executor generates disposable fixtures at runtime under the system temp directory.

No official migration, archive, operation, Edge Function, application source, `.env` file, or `supabase/.temp` file is mutated by these tests.
