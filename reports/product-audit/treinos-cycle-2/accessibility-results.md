# Accessibility Results

O editor expoe `role="dialog"`, `aria-modal`, resumo com `role="alert"`, `aria-invalid` e `aria-describedby` nos campos obrigatorios. O foco e movido para o primeiro erro relevante.
