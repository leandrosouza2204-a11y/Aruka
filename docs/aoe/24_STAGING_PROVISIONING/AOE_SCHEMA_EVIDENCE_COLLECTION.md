# AOE Schema Evidence Collection

Run the read-only SQL package from:

```text
scripts/aoe/sql/runtime-schema-inspection.sql
```

Export the result as JSON or CSV from a confirmed staging SQL Editor session.

The SQL package contains only read queries and filters AOE objects.
