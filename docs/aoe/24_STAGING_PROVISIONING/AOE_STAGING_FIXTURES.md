# AOE Staging Fixtures

Status: prepared, not executed.

Fixtures must use the `aoe_test_` prefix and be recorded in `.aoe-staging-fixtures.local.json`.

Creation command:

```bash
npm run aoe:staging:create-fixtures
```

The command blocks unless staging is confirmed.
