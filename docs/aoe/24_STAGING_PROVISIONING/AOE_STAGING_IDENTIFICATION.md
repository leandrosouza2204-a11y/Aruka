# AOE Staging Identification

Status: pending.

The current linked project ref is recorded only in masked form in reports.

A project can be used only when it is explicitly classified as `local`, `development`, or `staging` and confirmed as non-production.
