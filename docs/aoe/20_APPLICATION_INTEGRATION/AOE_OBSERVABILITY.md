# AOE Observability

Contratos:

- Logger: `debug`, `info`, `warn`, `error`.
- Metrics: `increment`, `observe`, `gauge`.

Eventos mínimos incluem decisão solicitada, decisão concluída, falha, idempotency hit, revisão criada, revisão concluída, autorização negada, request inválido e health check.
