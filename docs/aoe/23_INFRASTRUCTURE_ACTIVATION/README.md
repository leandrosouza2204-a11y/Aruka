# AOE v1.7.1 — Infrastructure Activation & Staging Validation

Status: NOT_READY.

O ambiente deste workspace não possui Supabase CLI reconhecido nem `supabase/config.toml`. Por segurança, migrations não foram aplicadas, Edge Functions não foram executadas e nenhum deploy foi realizado.

Entregas seguras nesta etapa:

- catálogo runtime pré-compilado;
- validação estática do bundle;
- relatórios de ativação;
- testes com skips explícitos para runtime ausente;
- documentação e ADRs.
