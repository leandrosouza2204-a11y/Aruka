# AOE Edge Runtime Integration

O runtime real da Edge Function não foi executado neste workspace. A função está preparada, mas o core AOE completo ainda precisa ser validado no runtime Supabase local/staging.
