# AOE Authentication Integration

A identidade autenticada via Supabase é a fonte oficial. O actor declarado no payload é tratado apenas como declaração e não pode sobrescrever o usuário autenticado.
