# AOE v1.7 — Infrastructure Adapters & Pilot Foundation

Status: READY_WITH_INFRASTRUCTURE_RESTRICTIONS.

O v1.7 adiciona migrations, RLS, adaptadores Supabase atrás dos ports do AOE, boundary server-side, feature flags e documentação operacional para piloto controlado.

Restrições: migrations não foram aplicadas, Edge Function não foi deployada e a execução do bundle AOE dentro da Edge Function deve ser conectada no ciclo de staging.
